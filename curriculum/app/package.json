{
  "name": "sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass --watch ./sass/main.scss ./main.css",
    "runall": "npm-run-all -p -l sass",
    "autoprefixed": "node-sass ./sass/layout.scss ./layout.css && postcss layout.css --use autoprefixer -o layout.css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^7.2.4",
    "node-sass": "^4.7.2",
    "npm-run-all": "^4.1.2",
    "postcss-cli": "^4.1.1"
  },
  "dependencies": {
    "aos": "^2.2.0"
  }
}
